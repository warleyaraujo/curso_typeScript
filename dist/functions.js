"use strict";
const sum = (x, y) => {
    return (x + y).toString();
};
const valueSum = sum(2, 3);
const log = (message) => {
    console.log(message);
};
const sub = (x, y) => {
    return (x - y);
};
const valueSub = sub(5, 3);
